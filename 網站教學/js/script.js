/* ==========================================================================
📂 PROJECT.CD | JAVASCRIPT DIRECTORY (腳本目錄索引)
==========================================================================
01. System Init ......... 系統初始化 (嚴格模式, 載入監聽)
02. Global Variables .... 全域變數定義 (暫存器, 狀態開關)
03. Utility Functions ... 工具函式 (共用的功能)
04. Event Listeners ..... 事件監聽 (點擊, 滾動, 鍵盤操作)
05. Main Execution ...... 主程式執行入口
==========================================================================
*/

// 【01. System Init: 啟動嚴格模式，防止變數未定義錯誤】
"use strict";

// 等待 HTML DOM 結構完全載入後，才執行 JS，避免找不到元素
document.addEventListener("DOMContentLoaded", () => {
    
    // 【05. Main Execution: 系統啟動歡迎訊息】
    // 使用 CSS 語法讓 Console Log 變成品牌色 (設計師的小巧思)
    console.log(
        "%c PROJECT.CD %c System Ready ", 
        "background: #000; color: #fff; border-radius: 3px 0 0 3px; padding: 2px 5px;", 
        "background: #ff5722; color: #fff; border-radius: 0 3px 3px 0; padding: 2px 5px;"
    );

    // 在這裡呼叫主程式
    initApp();
});

/* ==========================================================================
   Function Definitions (功能定義區)
   ========================================================================== */

/**
 * 應用程式主入口
 * 未來所有的功能初始化都放在這裡
 */
function initApp() {
    // 範例：之後我們會在這裡加入 setupMobileMenu();
    // 範例：之後我們會在這裡加入 setupScrollAnimation();
}